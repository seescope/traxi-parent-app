import * as Firebase from 'firebase';
import { updateKidImage } from '../../App/Containers/SetupCompletion';

describe('SetupCompletion', () => {
  it('updateKidImage', () => {
    // const kidImage = {
    //   uri: 'imageURL',
    // };
    // const selectedKid = {};
    // const kids = {};
    //
    // const dispatch = args => {};
    // updateKidImage(kidImage, selectedKid, kids)(dispatch);
    //
    // expect(Firebase.database().ref().update()).toHaveBeenCalledWith('ok');
    //
    // // expect(Firebase.database.ref.update).toHaveBeenCalled();
  });
});
