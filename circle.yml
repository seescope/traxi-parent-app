machine:
  node:
    version: 7.9.0

dependencies:
  override:
    - yarn

test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit
    - yarn test -- --runInBand
    - cp junit.xml $CIRCLE_TEST_REPORTS/junit/
